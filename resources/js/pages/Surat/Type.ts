export type LetterParams = {
    id: number;
    name: string;
    code: string;
    type: string;
    file?: string;
};

export type TypeLetterParams = {
    id: number;
    name: string;
    is_private: boolean;
};
